import { LastUpdatedDirective } from './last-updated.directive';

describe('LastUpdatedDirective', () => {
  it('should create an instance', () => {
    const directive = new LastUpdatedDirective();
    expect(directive).toBeTruthy();
  });
});
